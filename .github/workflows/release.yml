name: Release
on:
  push:
    tags: ['[0-9]*.[0-9]*.[0-9]*']
permissions:
  contents: read
jobs:
  validate:
    uses: ./.github/workflows/ci.yml
  interop:
    uses: ./.github/workflows/interop.yml
  publish:
    needs: [validate, interop]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          cache: npm
          cache-dependency-path: package.json
      - name: Resolve pinned SDK revision
        id: sdk
        run: echo "ref=$(node scripts/prepare-sdk.mjs --ref)" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v4
        with:
          repository: bchainhub/connect.js
          ref: ${{ steps.sdk.outputs.ref }}
          path: .connect-sdk
      - run: node scripts/prepare-sdk.mjs .connect-sdk
      - run: npm install --package-lock=false
      - name: Verify tag matches package version
        run: node -e 'if (process.env.GITHUB_REF_NAME !== JSON.parse(require("fs").readFileSync("package.json")).version) process.exit(1)'
      - run: npm run build
      - run: npm install -g npm@latest
      - run: npm publish --provenance --access public
      - run: gh release create "$GITHUB_REF_NAME" --verify-tag --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
